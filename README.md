# java-app
Java web app created using Docker container image and deploy using jenkins pipeline. It also uses webhooks to automatically trigger the pipeline whenver new commits pushed into Remote repository.
